interface:
  display_name: "Undo Snapshot"
  short_description: "Create and restore Git-backed edit snapshots"
  default_prompt: "Create an undo-snapshot checkpoint before modifying files in a Git worktree, and restore a named snapshot when requested."
