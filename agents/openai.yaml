interface:
  display_name: "Codex Undo"
  short_description: "Create and restore Git-backed Codex edit snapshots"
  default_prompt: "Create a codex-undo snapshot before Codex modifies files in a Git worktree. Use /undo <snapshot-name> to restore a saved state."
